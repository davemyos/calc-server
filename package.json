{
  "name": "server",
  "version": "1.0.0",
  "description": "server for calc app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "npm run start --prefix ../client",
    "start": "node index.js",
    "dev": "concurrently \"npm run start\" \"npm run client\""    
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "node": "^15.12.0",
    "socket.io": "^4.0.1"
  },
  "devDependencies": {
    "concurrently": "^6.0.1"
  }
}
